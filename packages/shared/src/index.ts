/**
 * `@poe2/shared` — zero-dependency primitives usable from the Electron main
 * process, the renderer and Node scripts alike. Nothing here may import
 * `electron`, `react` or any Node built-in.
 */
export * from './result.js';
export * from './text.js';
